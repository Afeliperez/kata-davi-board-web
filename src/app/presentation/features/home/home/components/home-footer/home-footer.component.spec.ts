import { HomeFooterComponent } from '@presentation/features/home/home/components/home-footer/home-footer.component';

describe('HomeFooterComponent', () => {
  it('creates component', () => {
    const component = new HomeFooterComponent();
    expect(component).toBeTruthy();
  });
});
